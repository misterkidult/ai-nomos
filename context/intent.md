# intent：這五天要做完什麼（2026-08-29）

> 這份不是新計畫。`context/plan-v2.md` 是定版計畫、`context/contract.md` 是資料契約，兩份都不因這份而改。
> 這份只回答一件事：**距離截止還剩五天，哪些事已經定了、哪些事還沒人拍板、哪些事我不做。**
> 待拍板的每一項都附我的建議選項，Kidult 只要說「照建議」或指定另一個。

---

## 一、時間與交件

截止 **9/3 13:00 PDT（台北 9/4 04:00）**。今天 8/29（六），實際工作日 **8/29–9/2 共五天**，9/3 只送件。

Devpost 交件四項的現況：

| 項 | 狀態 |
|---|---|
| ① 線上 URL | ✅ https://ai-nomos.vercel.app 活著。**但站上的資料與寫入路徑有缺口，見第三節** |
| ② 文字說明 | 🟡 `context/devpost-submission.md` 已起草，數字要更新（924→1314、374→341） |
| ③ <3 分鐘公開 YouTube 影片，要有聲音 | ❌ 沒開始。**這是唯一沒有替代方案的硬交件** |
| ④ 公開 repo ＋開源授權 | ✅ MIT 已在根目錄 |

「URL 與 repo 是連結不是快照」——報名後還能改站與程式碼，**只有影片與文案要在 9/2 前定稿**。

---

## 二、已經定了的（不再討論）

- 形態：字典本體是首頁，WebMCP 是它長大的方式。詞條主角是詞與定義句，來源退成註腳
- 儲存：Upstash Redis ＋ Vercel Function。`GET /api/sightings` 已上線，只回契約 §4 標 ★ 的公開欄位
- 三條鎖的參考實作是 `scripts/check-findings.py`，`api/*` 必須與它對 `fixtures/*.json` 判定一致
- 引文原文比對層已補上（`scripts/collect-fidelity.py`）。1526 筆比對結果：1314 忠實／191 不忠實／21 無法核對
- **入庫只收比對通過的 1314 筆**，191 筆不上線（Kidult 08-29 裁示）
- **契約 §6 的偏離不算數**——subagent 直接產出、沒走過 `/read` 的那兩輪不承認，replay 必須真的走頁面（Kidult 08-29 裁示）
- 英譯走單獨 translations 表，不拿原文當 key（尚未建）
- 目擊不回寫 `ai-dictionary` 的 `terms/`；沒有 seed
- 部署只走 prebuilt，GitHub 連結保持斷開

---

## 三、最大的缺口：寫入路徑目前不存在

查證後的事實，不是推測：

1. `public/read.html` 的 `submitFindings` **完全在瀏覽器裡跑完**——跑三條鎖、更新畫面、回 `{status:'mock'}`，**沒有任何一行送往伺服器**
2. `api/` 底下只有 `sightings.js`（唯讀 GET）。**沒有寫入端點**
3. 線上那 1314 筆是 `scripts/kv-load.py` 直接灌進 Upstash 的，**同樣沒走過頁面**

也就是說：契約 §6 說「唯一的編輯動作是提交一筆目擊」，而**那個動作現在還沒有伺服器端**。08-29 裁示要求 replay 走真實路徑，這條路徑得先存在。

這是我認為五天內第一優先的事，理由不是完整度而是評分：四個評分項裡的 **WebMCP Leverage** 就在這條路徑上。評審打開站、叫 agent 讀一篇文章、送回——如果送回只是畫面上動一下、重整就沒了，作品的核心主張沒有被看見。

附帶的環境事實：`collect/`（queue.json ＋ 374 個 docs）在 `.gitignore` 裡，**只存在 Kidult 的機器上**，這個 session 的 clone 沒有。要 replay 得先把它放進來。

---

## 四、六題已拍板（Kidult 08-29：「照做」＝全數採納建議）

**① 寫入端點做不做？做到哪一層？**
- (a) 做 `POST /api/findings`：伺服器跑三條鎖＋停用清單＋regex 攔截＋速率限制，`read.html` 改成真的送出 ← **✅ 採納**
- (b) 只做端點不做 replay：新目擊能寫入，1314 筆維持直灌
- (c) 不做，維持 mock，靠影片演

**② 1314 筆要不要重跑 replay 走真實路徑？**
- (a) 不重跑。端點做好之後，**現場餵幾篇新文章**走完整路徑，直灌的當歷史資料 ← **✅ 採納**（五天內重播 341 篇不現實，而且 §6 的精神是「這扇門存在且所有人走同一扇」，不是「每一筆都要重演」）
- (b) 全部重跑
- (c) 抽一批（例如 20 篇）重跑當佐證

**③ 191 筆不忠實的，賽前處理嗎？**
- (a) 不處理，維持不上線，在 README／文案裡把這件事講成賣點——「我們有一層驗證抓出自己 12.7% 的資料不忠於原文，所以沒讓它上線」 ← **✅ 採納**
- (b) 救 `reassembled` 那 62 筆
- (c) 重跑 88 篇

**④ UI 張力那一輪（`context/mock/` 的假畫面）要不要搬進 `public/`？**
- (a) 要，但排在寫入端點之後 ← **✅ 採納**
- (b) 優先於寫入端點
- (c) 賽後再說

`plan-ui-tension.md` §7 那三題（Agent 頁 16 種寫法只留前 5、動態牆同篇文章合併、契約加 “translations are a display layer”）**仍未答**，8/30 動 UI 前要答。

**⑤ `public/sightings.json`（510 KB）刪不刪？**
- (a) 留著當 API 故障時的退路，直到 9/2 影片錄完再刪 ← **✅ 採納**
- (b) 現在就刪（CLAUDE.md 原本寫 8/29 匯入後刪）

**⑥ 影片誰錄、什麼時候？**
- ✅ **9/1（一）先錄一版粗剪**，9/2 補錄修正。不要壓在 9/2 一天
- 腳本沿用 `plan-v2.md` §五，但第 1、2 幕換成走真實寫入路徑的畫面

---

## 五、我明確不做的

- 不改契約 §1–§3（改了要 bump version，五天內不值得）
- 不做 `/review` 整頁、不做讀者投票、不做公開 API／MCP server
- 不補那 5 個 `doc_count<3` 的缺口、不重試 12 篇抓取失敗的
- 不碰配色字型風格的討論
- 假畫面沒拍板前不碰 `public/`
- 不重新連接 Vercel 的 GitHub

---

## 六、若六題全照建議，五天長這樣

| 日 | 交付 | 驗收 |
|---|---|---|
| **8/29 六** ✅ | 這份 intent 拍板；`POST /api/findings` 寫入端點（三條鎖與 `check-findings.py` 對齊）；`read.html` 的 `submitFindings` 改成真的送出 | 判定與參考實作逐字一致 ✅；端點測試 20 項 ✅；瀏覽器全流程 12 項 ✅。**剩線上實測**：部署後在 ChatGPT 桌面版餵一篇真文章，確認資料進 Upstash、重整還在 |
| 8/30 日 | UI 張力搬進 `public/`（先 `/term` 再首頁）；`plan-ui-tension.md` 三題落地 | 線上點得到，破版 0 |
| 8/31 一 | translations 表；英文層進 `public/`；文案數字更新 | zh／en 兩套都對 |
| 9/1 二 | **影片粗剪**；ChatGPT 桌面版跑完整腳本一次；buffer | 影片 <3 分鐘、有聲音 |
| 9/2 三 | 影片定稿上 YouTube（公開）；Devpost 文案定稿；最後一次 prebuilt 部署 | 四項交件齊備 |
| 9/3 四 | 送出（台北時間 9/4 04:00 前） | Devpost submitted |

日程有 buffer 的地方只有 9/1。**如果 ① 選了 (b) 或 (c)，8/29 這格會空出來，整個日程往前一天，風險降低但 WebMCP Leverage 那項的分數也跟著降。這是這五天唯一真正的取捨。**


---

## 七、拍板紀錄

**2026-08-29 Kidult：「好，照做」** —— 六題全數照建議。展開後等同以下六條裁示：

1. 做 `POST /api/findings`：伺服器跑三條鎖＋停用清單＋PII regex＋速率限制，`read.html` 的 `submitFindings` 改成真的送出
2. 1314 筆不重跑 replay；端點做好後現場餵新文章走完整路徑，直灌的當歷史資料
3. 191 筆不忠實的維持不上線，在文案裡講成賣點
4. UI 張力搬進 `public/`，排在寫入端點之後
5. `public/sightings.json` 留到 9/2 錄完影片再刪
6. 影片 9/1 錄粗剪、9/2 定稿

第六節的五天日程即為執行順序。**掛著沒答的只剩 `plan-ui-tension.md` §7 那三題**，8/30 開工前要答。


---

## 八、8/29 收工紀錄

寫入路徑做完了。第三節那個缺口（契約 §6 的唯一編輯動作沒有伺服器端）已經補上。

- `api/findings.js` —— `POST /api/findings`。跑三條鎖＋停用清單＋PII regex＋速率限制，通過的寫進 Upstash，鍵值與 `scripts/kv-load.py` 同一套，所以 8/29 那批與線上新收的是同一份資料
- `api/_locks.js` —— 判定抽成一份，`api/*` 與測試共用。**改鎖要三處同步**：契約 §3、`scripts/check-findings.py`、這支
- `public/read.html` —— `submitFindings` 真的送出了。伺服器的判定才算數（PII 那條只有伺服器有）；**連不到伺服器時回 `status:'mock'` 並在畫面上說「未寫入」**，不假裝存到了。「載入範例」那顆按鈕不寫入

三層驗證，都在 `CLAUDE.md` 的「規則」段有指令：

| 驗什麼 | 怎麼驗 | 結果 |
|---|---|---|
| 鎖的判定與 Python 參考實作一致（契約 §3 的要求） | `diff` 兩支 CLI 對 `fixtures/locks-v1.json` | 逐字一致 |
| 端點 | `scripts/test-findings.mjs`（假 Upstash，免憑證） | 20 項全過 |
| `/read` 全流程 | `scripts/test-read-e2e.mjs`（真 Chromium，假 `modelContext`） | 12 項全過 |

新增 `fixtures/locks-v1.json`：契約 v1 形狀的鎖回歸輸入，每個拒絕碼各一筆，外加一組對照（同樣含電話與金額的文字，貼上的被 `PII_DETECTED` 擋、公開文章的照契約 §2 規則 5 放行）。**只是回歸輸入，不入庫**（CLAUDE.md：沒有 seed）。

### 兩件要你知道的

1. **同一篇文章重餵不會重複計數。** 一則目擊的 id 由（文件、詞）決定，重送就地覆蓋 —— 契約 §2 規則 6 說一篇文章一個詞只報一次。但 8/29 直灌那 1314 筆的 id 是舊格式 `<job_id>-<index>`，對不上。已附 `scripts/kv-backfill-docterm.py`，**要你在有 `.env` 的機器上跑一次**，那 341 篇之後重餵才會覆蓋而不是長出第二筆
2. **契約 §4 說 `source.hash` 是「文件內文的加鹽雜湊、客戶端算」，端點對公開文章改用 `sha256(url)`。** 理由：URL 本來就是 ★ 公開欄位，加鹽保護不到任何東西，而且這樣才和已入庫的 341 篇對得上；貼上的文件沒有 URL，仍收客戶端送的雜湊。**這與契約字面有出入，要不要改契約字面是你的決定**（走 `contract:` commit，不在聊天裡改）

### 一個提案，沒做

今天的 `fidelity-report` 證明三條鎖看不到「這句話是否真的在頁面上」—— 191 筆就是這樣混進來的。端點其實可以在寫入前抓一次來源頁做同一件事，把 `collect-fidelity.py` 那層搬到線上。**沒做**：要加一個拒絕碼＝改契約 §3，而且伺服器去抓文章與「文章不經過我們的伺服器」的說法要先想清楚（抓公開 URL 與收使用者的文件是兩回事，但這個分別要講得出來）。以評分來說這是很強的一張牌，**要不要做、9/1 的 buffer 夠不夠，你決定**。

---

## 九、第三個意圖：第一次來的人，要知道自己能用 WebMCP 做什麼

> Kidult 08-29 追加。前八節談的是「這個作品要成立」，這一節談的是「這個作品要被看懂」。

前面兩個意圖是**字典要長得對**（詞與定義句是主角）與**這扇門要真的存在**（契約 §6 的寫入路徑，8/29 已補上）。第三個意圖不同：它衡量的不是資料或程式，是**一個素昧平生的人打開這一頁之後的三十秒**。

這件事對參賽的份量不低於前兩者。四個評分項裡，Execution 與 Potential Impact 都落在這裡；而 Addy 8/27 審查說最弱的那項「有用」，弱的正是這一段。**評審打開站的時候，他就是第一次來的人。**

### 兩個主要工作流（Kidult 指定）

| | 工作流 | 使用者拿到什麼 |
|---|---|---|
| **一** | 貼上一篇文章，分出**「已經有的字庫」**與**「可能是新的字庫」** | 這篇文章裡，哪些詞字典已經收了、字典的說法跟這篇一不一樣；哪些詞是字典還沒有的 |
| **二** | 這篇文章分析完之後**匯入資料庫**，成為日後可查詢的字庫 | 我剛剛讓字典長大了：進去了幾筆、哪些被擋下與為什麼、以及我現在就能去查我貢獻的那個詞 |

兩者都不是新功能 —— 底層今天都已經在了（工作流一＝`feedDocument` ＋ 本地比對；工作流二＝`submitFindings` → `POST /api/findings` → `GET /api/sightings`）。**缺的是讓人看得懂它在發生。**

### 第三條：讓一般人也覺得有趣

「有趣」不是動畫或配色（Kidult 8/27 已明講不是那個）。判準沿用 `plan-ui-tension.md`：**兩個數字或兩句話擺在一起才有意思，單一數字沒有。**「這個詞被目擊 16 次」不有趣；「16 篇在賣它、7 篇在擔心它」才有趣。

一個一般人不會為了「AI 詞彙表」回訪，但會為了「原來大家講的不是同一個東西」多看兩眼。**有趣是可用性的前提，不是裝飾。**

### 驗收：怎麼知道做到了

不看畫面好不好看，看這五句話一個沒用過的人講不講得出來：

1. 這個站是幹嘛的 —— 一句話，不用捲頁
2. 我現在能做什麼 —— **三種訪客都要有下一步**：有 agent 的、有環境但沒開權限的、完全沒有 agent 的（99% 的訪客是第三種，退化模式不能是死路）
3. 我要對我的 agent 說什麼 —— 不能要他自己憑空想一段話
4. 我剛剛做了什麼 —— 送出之後知道字典長大了多少，被擋下的知道為什麼（**而且知道那不是他的錯，是 agent 沒照抄原文**）
5. 我之後去哪裡查 —— 從「我剛送出」走得到「我貢獻的那個詞」的詞條頁

### 與日程的關係

**這一節與裁示 ④ 的 UI 張力是同一格時間（8/30–8/31），要合流，不是兩條線各做各的。** UI 張力解決「詞與定義句是主角」，這一節解決「第一次來的人看不看得懂」—— 同一批畫面上的兩個問題，分開做會打架。

`plan-ui-tension.md` §7 掛著的三題仍未答，動手前要答。

### 一個要當心的地方

引導文案很容易寫出沒實測過的技術宣稱。ChatGPT 與 Chrome 的 WebMCP 行為，只能引 `context/devpost-rules-2026-08-27.md` 裡 8/27 實測過的部分 —— 包括 `requestUserInteraction` 已被移出規格這件事（Addy 審查那句已撤回）。**沒測過的就寫「未驗證」，不要為了文案好看而編。**這個作品的賣點是「可回頁面核對」，說明文字自己先造假就完了。
